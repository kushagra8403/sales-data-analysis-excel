# Data

The portfolio dataset is synthetic and generated reproducibly with `src/generate_data.py` using a fixed random seed.

Run:

```bash
python src/generate_data.py
```

This creates `data/sales_data.csv` with 3,000 synthetic UK retail orders covering January 2024 to December 2025.
